# Existing values
DATABASE_URL="postgresql://postgres:password@db-user:5432/user?schema=public"
REDIS_HOST=redis
REDIS_PORT=6379

# New values for email service (keep these in your secure environment)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM=noreply@yourapp.com
CLIENT_URL=http://localhost:3000
PASSWORD_RESET_TOKEN_EXPIRES_IN=15m